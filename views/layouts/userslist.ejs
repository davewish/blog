<!DOCTYPE html>
<html lang="en">

  <head>

    <title>Document</title>
  </head>

  <body>
    <% locals.list.map((user)=>{%>
      <div>
        name:<%=user.name%>
          email:<%=user.email%>
            <%if(user.avatar){%>
              <img width="50px" height="50px" src=<%=user.avatar%> />
              <%}%>
      </div>
      <%})%>
  </body>

</html>